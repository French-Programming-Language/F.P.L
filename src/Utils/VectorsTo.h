#include <iostream>
#include <string>
#include <vector>
#include <map>
#include <optional>
#include <sstream>
#include <iterator>

namespace FPL::Utils {
    std::string StringVectorToString(std::vector<std::string>& vec);
}